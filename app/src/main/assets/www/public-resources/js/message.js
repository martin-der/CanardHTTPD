
var message = {
	theme_loading : "Loading theme '{THEME}'...",

	shared_object_not_found : "That share doesn't seem to exist anymore."
};

if (typeof Object.freeze == 'function') {
	Object.freeze(message);
}
